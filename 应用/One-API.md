**报告日期**：2026-02-22 **核心定位**：将本地 3090 算力标准化，实现“一处供给，多端消费”。
---

## 1. 物理层与网络配置 (The Infrastructure)

为了确保算力能够跨越物理距离输送到你的开发终端，我们构建了三活冗余路径：

- **算力节点**：3090 算力中心，通过 `8189` 端口提供 OpenAI 标准的原始接口。
    
- **虚拟内网 (优先)**：通过 **ZeroTier** 建立 `10.210.8.8:3001` 通道，实现无公网 IP 下的加密直连。
    
- **公网弹性 (备选)**：
    
    - **固定 IP**：`101.43.29.236:3001`（待端口映射激活）。
        
    - **临时隧道**：Cloudflare `trycloudflare` 随机域名（用于紧急调试）。
        

---

## 2. One-API 逻辑配置 (The Controller)

在 One-API 管理后台，我们完成了从“原始接口”到“内阁令牌”的封装：

### A. 渠道设置 (Channels)

- **类型**：OpenAI / OpenAI-like。
- **代理地址**：指向 3090 本地的 `http://localhost:8189`（或内网地址）。
- **模型映射**：将 3090 上的豆包或其它国产大模型统一映射为 `gpt-4o` 或 `claude-3-5-sonnet` 等标准 ID，确保 Cursor 能够识别。

### B. 令牌分发 (Tokens)

- **内阁全量令牌**：`sk-6xV5...`（已在各个工具端同步生效）。
- **权限限制**：当前设置为无限制，未来可根据孩子学习 AI 的进度，设置额度限制以监控显存负载。
    

---

## 3. 应用端集成 (The Consumers)

我们将生成的 One-API 令牌无缝接入了两个核心生产力工具：
### 🛠️ Cursor 集成

- **Base URL**: `http://10.210.8.8:3001/v1`（利用 ZeroTier 通道）。
- **API Key**: 使用 One-API 生成的内阁全量令牌。
- **效果**: Cursor 现在通过你的 3090 进行代码补全和 Composer 重构。

### 🛠️ Continue 集成 (小酷/小康)

- **Provider**: `openai` (通过 One-API 模拟)。
- **配置层级**: 在 `config.yaml` 中将 `apiBase` 指向 One-API 地址。
- **功能**: 支持 `@codebase` 检索和终端报错诊断，算力开销由 One-API 统一计量。

---

## 4. 运行风险与监控

- **显存负载**：24GB VRAM 是核心瓶颈。如果 Cursor 进行大规模索引的同时进行高频对话，需通过 One-API 观察响应延迟。
    
- **端口安全**：一旦公网 `101.43.29.236:3001` 开启，必须在 One-API 中启用强密码保护，防止算力被外部盗用。



---

# ⚡️ 内阁算力调度中心：豆包 (Doubao) 专项配置报告

**报告日期**：2026-02-22

**核心定位**：将字节跳动提供的“豆包”模型能力，本地化封装并分发。

---

## 1. 豆包模型准入 (Upstream Configuration)

在 One-API 的 **渠道 (Channel)** 模块中，豆包的接入逻辑如下：

- **供应商类型**：字节跳动 (ByteDance / Doubao)。
    
- **基础 URL**：`https://ark.cn-beijing.volces.com/api/v3` (火山引擎 Ark 平台标准接口)。
- **模型标识 (Model ID)**：
    - **主力型**：`ep-xxxxxx-doubao-pro-32k` (用于代码审计、逻辑分析)
    - **轻量型**：`ep-xxxxxx-doubao-lite-4k` (用于快速代码补全)。
- **API Key**：内阁专属火山引擎 API 密钥。
    

---

## 2. One-API 转发与路由 (The Bridge)

豆包并不是直接被客户端调用的，它在 One-API 内部经过了“内阁化”处理：

- **多模型聚合**：我们将多个豆包 Endpoint 聚合在一个 One-API 渠道下，实现了 **自动负载均衡**。
    
- **ID 翻译 (Model Mapping)**：
    
    - 由于 Cursor 有时只认 `gpt-4o` 或 `claude-3-5-sonnet`，我们在 One-API 里设置了映射逻辑：
        
    - `doubao-pro` ➔ **映射为** ➔ `gpt-4o`
        
    - 这样你不仅省了钱，还骗过了那些“只认名牌”的客户端。
        

---

