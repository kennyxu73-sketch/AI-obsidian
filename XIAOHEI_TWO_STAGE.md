# 小黑两阶段流程说明

> 本地 3090 做**初步筛选**生成「文件缩影」→ 云端商业算力做 **JSON 深度输出**，兼顾隐私与成本。

---

## 一、整体流程

```
原始内容（笔记/周记/文章/思维导图等）
        │
        ▼
┌───────────────────────────────────────┐
│  Step 1 · 本地 3090（初步筛选）          │
│  输出：文件缩影（字数、约200字概要、类型） │
└───────────────────────────────────────┘
        │
        ▼ 缩影（+ 可选：原文摘要/全文）
┌───────────────────────────────────────┐
│  Step 2 · 云端商业算力（Dify 等）        │
│  输入：缩影 + 可选原文                   │
│  输出：完整小黑 JSON（归档/路由/风险等）  │
└───────────────────────────────────────┘
        │
        ▼
  小酷 harvester 落盘 Obsidian
```

---

## 二、Step 1 输出：文件缩影（Schema）

本地 3090 只做轻量分析，**固定输出**以下结构（便于 Step 2 或工作流解析）：

| 字段 | 类型 | 说明 |
|------|------|------|
| `word_count` | number | 文字数量（中英文均按字/词计，可近似） |
| `summary_200` | string | 大致内容，**约 200 字**以内 |
| `file_type` | string | 文件类型枚举，见下表 |
| `source_path` | string | 可选，原始文件路径或 ID，便于 Step 2 关联 |

### file_type 枚举（Step 1 只选其一）

| 取值 | 含义 |
|------|------|
| `note` | 普通笔记 |
| `weekly_journal` | 周记/周总结 |
| `web_article` | 网页/公众号/文章 |
| `video_note` | 视频笔记 |
| `mindmap` | 思维导图 |
| `canvas` | Canvas/白板 |
| `other` | 无法判断 |

### 缩影 JSON 示例

```json
{
  "word_count": 1250,
  "summary_200": "本周主要完成产品需求评审与两次对外会议；下周计划推进慰康试点落地与憶镜叙事模块设计。含 3 条待办、2 条复盘。",
  "file_type": "weekly_journal",
  "source_path": "Inbox/2025-02-20 周记.md"
}
```

---

## 三、Step 1 实现（本地 3090）

- 脚本：**`xiaohei_synopsis.py`**（见项目内）。
- 输入：原始文本（或文件路径，脚本内读入）。
- 调用：本地 3090 `v1/chat/completions`，用**短 prompt** 要求只输出上述 4 个字段的 JSON。
- 输出：标准缩影 JSON，可落盘或直接交给 Step 2。

Step 1 **不**做：完整小黑 JSON、agent_relations、risk_flags、archive_recommendation 等，全部留给云端 Step 2。

---

## 四、Step 2 输入（云端 Dify）

云端小黑节点的**推荐输入**：

1. **必填**：Step 1 的**缩影 JSON 字符串**（或已解析后的 `word_count`、`summary_200`、`file_type`、`source_path` 文本描述）。
2. **可选**：  
   - 原文全文（若云端允许且需要深度抽取）；  
   - 或仅「缩影 + 原文前 N 字」，以控制 token 与成本。

Dify 中可将「Step 1 输出」作为变量传入小黑 System Prompt 所规定的输入（如 `{{#1771558348994.input#}}`），并在 Prompt 开头说明：

> 你收到的是「文件缩影」：字数、约200字概要、文件类型。请基于缩影（及所附原文片段，如有）输出完整小黑 JSON。

这样云端只对「缩影 + 可选原文」做深度推理，无需在云端再跑一遍全文统计与粗分类。

---

## 五、与现有组件的衔接

| 环节 | 说明 |
|------|------|
| Step 1 | `xiaohei_synopsis.py`，读原文 → 调 3090 → 输出缩影 JSON。 |
| Step 2 | Dify 小黑：输入 = 缩影（+ 可选原文）；输出 = 现有完整小黑 JSON Schema。 |
| 落盘 | 与现在一致：小黑 JSON → `harvester.py` → 表头 + 归档目录 + Dataview。 |

若希望「仅缩影就决定不归档」，可在 Step 1 后加简单规则（如 `word_count < 50` 且 `file_type == other` 则跳过 Step 2），再按需实现。
