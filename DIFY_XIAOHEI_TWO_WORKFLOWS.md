# Dify 端：小黑两阶段 = 2 个工作流 + 2 套 Prompt

> 工作流一：跑 **3090**，只做初步筛选，输出「文件缩影」。  
> 工作流二：跑 **云端算力**，以缩影（+ 可选原文）为输入，输出完整小黑 JSON。

---

## 一、整体关系

```
                    ┌─────────────────────────────────────┐
                    │  Dify · 工作流一「小黑·初步筛选」      │
 原始内容 ──────────►│  模型：3090（自定义 OpenAI API）      │
 {{content}}        │  Prompt：只输出缩影 JSON              │
                    └─────────────────────────────────────┘
                                        │
                                        ▼ 缩影 JSON 字符串
                    ┌─────────────────────────────────────┐
                    │  Dify · 工作流二「小黑·深度分析」      │
                    │  模型：云端商业模型                   │
 可选：原文 ────────►│  Prompt：基于缩影输出完整小黑 JSON   │
 {{raw}}            └─────────────────────────────────────┘
                                        │
                                        ▼ 完整小黑 JSON
                             小酷 harvester 落盘
```

- **工作流一**：必须用「自定义模型」或「OpenAI 兼容 API」指向你的 3090 地址。  
- **工作流二**：用 Dify 自带云端模型即可；输入变量 = 工作流一的输出（缩影）+ 可选原文。

---

## 二、工作流一（3090 · 初步筛选）— Prompt

### 2.1 节点与变量

- **开始节点**：至少一个输入变量，例如：
  - `content`（必填）：待分析的原始文本。
  - `source_path`（可选）：来源路径，便于写入缩影的 source_path。
- **LLM 节点**：选择「自定义 / OpenAI 兼容」模型，API 填 3090 的 `v1/chat/completions` 地址（如 `http://101.43.29.236:8189/v1`），模型名与 3090 上一致（如 `internlm3-8b-instruct`）。
- **输出**：该 LLM 节点的回复即为「缩影 JSON 字符串」，可命名为 `synopsis`，供工作流二使用。

### 2.2 系统提示词（System Prompt）— 直接复制

```
你是「小黑·初步筛选」环节。只做一件事：对用户给的一段内容做轻量分析，输出一个 JSON，且只输出这一个 JSON，不要任何其他文字、不要 Markdown 代码块、不要解释。

JSON 必须且仅包含以下 4 个字段（字段名必须是英文）：
1. word_count: 整数字，表示这段内容的「文字数量」大致多少（中英文都算）。
2. summary_200: 字符串，用大约 200 字以内概括这段内容在讲什么、有什么关键信息。
3. file_type: 字符串，且只能是以下之一：note, weekly_journal, web_article, video_note, mindmap, canvas, other。
4. source_path: 字符串，若没有来源路径就填空字符串 ""。

要求：
- 只输出合法 JSON，不要 markdown 代码块包裹，不要解释。
- file_type 根据内容判断：周记/周总结用 weekly_journal；网页/文章用 web_article；思维导图结构用 mindmap；白板/画布用 canvas；视频相关用 video_note；普通笔记用 note；无法判断用 other。
```

### 2.3 用户消息（User / Query）— 模板

在 Dify 的「用户输入」或对应变量里，用类似内容组出最终发给 3090 的「用户消息」：

```
来源路径（若无则忽略）：{{#source_path#}}

待分析内容：

{{#content#}}
```

- `{{#content#}}`：来自工作流输入变量（原始文本）。  
- `{{#source_path#}}`：可选，来自工作流输入变量。

这样工作流一的**输出**就是一段「只含一个 JSON 对象」的文本，即文件缩影。

---

## 三、工作流二（云端 · 深度分析）— Prompt

### 3.1 节点与变量

- **开始节点**：  
  - `synopsis`（必填）：工作流一输出的「缩影 JSON 字符串」（或从外部传入的同等结构）。  
  - `raw_content`（可选）：原文或原文前 N 字，用于深度抽取事实与要点。
- **LLM 节点**：选择**云端商业模型**（如 Dify 默认或你配置的 GPT/Claude 等）。  
- **输入**：把 `synopsis`（以及可选的 `raw_content`）拼成一段话，作为「用户输入」传给该 LLM；系统提示词用下面的「完整小黑」版本。

### 3.2 系统提示词（System Prompt）— 与现有完整版一致 + 两阶段说明

工作流二的系统提示词 = **`DIFY_XIAOHEI_SYSTEM_PROMPT.md` 里从「以下为 Dify 系统提示词」到「Return ONLY a valid JSON...」的整段**，其中已包含：

- 若输入是「文件缩影」（含 word_count、summary_200、file_type、source_path），则**基于该缩影**（及所附原文片段，如有）完成分析并输出完整小黑 JSON，不必再统计字数或重做粗分类。

因此**无需改现有完整版 Prompt**，直接整段复制到工作流二的「系统提示词」即可。

### 3.3 用户消息（User / Query）— 模板

在工作流二中，把「缩影 + 可选原文」组在一起发给云端模型，例如：

```
以下是一份「文件缩影」与可选原文，请基于此输出完整小黑 JSON（仅输出一个合法 JSON 对象）。

【缩影】
{{#synopsis#}}

【原文（可选，若无则忽略）】
{{#raw_content#}}
```

- `{{#synopsis#}}`：工作流一的输出，或外部传入的缩影 JSON 字符串。  
- `{{#raw_content#}}`：可选；若为空，云端仅根据缩影做推断与归档建议。

工作流二的**输出**即为完整小黑 JSON，可再接「代码节点」做校验或直接交给小酷 harvester 落盘。

---

## 四、两套 Prompt 对照小结

| 项目 | 工作流一（3090 · 初步筛选） | 工作流二（云端 · 深度分析） |
|------|-----------------------------|-----------------------------|
| **模型** | 3090（自定义 OpenAI 兼容 API） | 云端商业模型 |
| **输入** | 原始内容 `content` + 可选 `source_path` | 缩影 `synopsis` + 可选原文 `raw_content` |
| **System Prompt** | 本节 2.2：只输出 4 字段 JSON（字数、约200字概要、file_type、source_path） | 使用 `DIFY_XIAOHEI_SYSTEM_PROMPT.md` 完整版（已含「输入为缩影」说明） |
| **User 模板** | 本节 2.3：来源路径 + 待分析内容 | 本节 3.3：缩影 + 原文（可选） |
| **输出** | 缩影 JSON 字符串 | 完整小黑 JSON 对象 |

---

## 五、串联方式（Dify 内 vs 外部）

- **全在 Dify 内**：工作流一跑完后，用「工作流」或「对话」的返回值把 `synopsis` 作为工作流二的输入变量；若需要原文，可从工作流一的输入变量再传一份到工作流二。  
- **Step 1 在外部**：若 Step 1 用本地脚本 `xiaohei_synopsis.py` 或 n8n 调 3090，则只需在 Dify 里建**工作流二**，其输入变量改为「由外部传入的缩影 + 可选原文」即可；Prompt 仍用同一套（本节 3.2 + 3.3）。

这样你在 Dify 端要做的是：**2 个工作流 + 2 套 Prompt**（一简一全），按上表配置即可。
