

---

# 🛡️ AI-OB 外部交互层协议 (External Interface Protocol)

**版本：** 1.1

**状态：** 核心规约 (Mandatory)

**适用范围：** 跨端数据流转、自动化脚本逻辑、AI 代理访问控制

---

## 一、 系统分层架构 (Layered Architecture)

本协议将系统划分为两个物理与逻辑隔离的层级，确保核心知识库的绝对安全。

### 1. L1 核心资产层 (Core Assets Layer)

- **Obsidian (SSOT)**:
    
    - **定义**: 系统唯一事实来源（Single Source of Truth）。
        
    - **职能**: 存储经人工审定的结构化知识、项目档案、核心日记。
        
    - **访问限制**: AI 代理仅拥有“只读”或“追加”权限；物理移动与覆盖写操作必须通过审计触发。
        
- **Index Layer (元数据索引)**:
    
    - **定义**: Obsidian 的影子查询层。
        
    - **职能**: 定期扫描 L1 文件，提取标签、关系与双链。
        
    - **逻辑**: 提供高性能 API 供 L2 终端查询，避免直接 IO 冲击。
        

### 2. L2 交互缓冲层 (Interaction Buffer Layer)

- **Input (输入端)**: 微信自然对话、语音采集、灵感快记。
    
- **Processor (处理端)**: Dify Skill / CLI 工具流。负责数据清洗、实体提取与初步分级。
    
- **Auditor (审计端)**: 微信公众号/企业微信。作为“人在环路”的唯一物理确认点。
    
- **Viewer (展示端)**: H5 全局看板。提供跨文件聚合视图。
    

---

## 二、 核心数据流规约 (Standard Data Flows)

### 1. 增量捕获协议 (Capture Protocol)

- **路径**: `WeChat` ➔ `Skill` + `Obsidian(/inbox/raw)`
    
- **逻辑**: 捕获用户意图时，必须同时生成两个分身：
    
    1. **原始存档**: 实时、无损地存入 Obsidian 临时收件箱。
        
    2. **结构化任务**: 触发异步 Skill 进行语义处理。
        
- **标识**: 每一条捕获记录必须包含 `transaction_id` 以防重复处理。
    

### 2. 人在环路审计协议 (HITL Protocol)

- **路径**: `Skill` ➔ `WeChat_MP` ➔ `Obsidian`
    
- **逻辑**:
    
    1. **分级过滤**: 仅 A/B 级高价值信息触发此协议。
        
    2. **挂起审核**: 在 MP 端展示结果，状态标记为 `Pending`。
        
    3. **确认执行**: 接收到“确认”指令后，由系统执行物理位移，从临时缓冲区移至正式库。
        

### 3. 存量激活协议 (Refining Protocol)

- **路径**: `Obsidian(L1)` ➔ `Skill(L2)`
    
- **逻辑**: Skill 被授权定期或受托读取 L1 指定目录，进行“存量提纯”。
    
- **禁忌**: 严禁在抽取加工期间对原文件进行非预期的结构破坏。
    

### 4. 实时状态回馈协议 (Live-Feed Protocol)

- **路径**: `Skill` ➔ `H5`
    
- **逻辑**: 跳过索引更新，将任务进度、实时分析片段直接推送至展示端，确保交互的即时性。
    

---

## 三、 运行安全红线 (Safety & Governance)

### 1. 路径隔离原则

- 禁止任何 L2 终端直接暴露服务器物理路径。所有路径必须进行符号化处理（如：`{VAULT}/Daily/`）。
    

### 2. 写入锁定与熔断

- **操作冲突**: 当 L1 文件处于“编辑态”时，所有 `Skill` 的写入请求必须进入 `Queue` 或触发 `error_feedback`。
    
- **熔断条件**: 若自动化流检测到数据校验失败（Triple-A 结构失效），必须立即生成 `.halt` 文件并向 `WeChat` 发送 🔴 级别告警。
    

### 3. 数据幂等性

- 所有写入 L1 库的操作必须可重入且不产生副本。执行前必须校验 `file_hash` 或 `event_id`。
    

---

## 四、 状态指示定义 (Visual Status Definitions)

为了在日志和管理端保持一致，统一状态语义：

- 🔴 **Critical / Pending**: 自动化挂起，需统帅（Kenny）介入决策。
    
- 🟠 **Processing**: 计算引擎激活中，正在进行 L1/L2 跨层数据交换。
    
- 🟢 **Completed**: 任务落盘，索引同步完成，系统恢复稳态。
    

---

