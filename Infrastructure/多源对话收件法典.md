---
title: 多源对话收件法典
role: SSOT · 与 TOOLS dialog_inbox.py 模块注释同源
updated: 2026-03-29
---

# 多源对话收件法典

> **同源**：[`Internal_Cabinet_Tools/dialog_inbox.py`](file:///Volumes/Cabinet/cabinet/Cursor_Workspace/Internal_Cabinet_Tools/dialog_inbox.py) 模块顶部文档字符串与本文件一致；修订须双处同步。

## 落盘

- 会话正文：`RUNTIME_ROOT/ai_dialogue_inbox/*.md`
- 索引：`RUNTIME_ROOT/ai_dialogue_inbox/inbox_manifest.jsonl`（每会话一行 JSON）
- 适配器故障日志：`RUNTIME_ROOT/ai_dialogue_inbox/sync_errors.log`

## 1. 增量追加协议（Incremental Protocol）

- **规则**：严禁默认覆盖。以 **`source` + `session_id`** 为全局唯一键。
- **动作**：`append_turn` 时，若目标会话文件已存在，**仅在文件末尾**追加新的 `## User` / `## Assistant` 块（含法定 `<!-- captured_at: ... -->`），并 **强制** upsert `inbox_manifest.jsonl` 的 **`last_captured_at`**。
- **意义**：长对话多次同步保持完整时间线，避免整文件重复拷贝。

## 2. 故障隔离红线（Fault Isolation）

- **规则**：单点崩溃，全局继续。
- **动作**：`run_sync_all()` 对每个 Adapter **单独** `try` / `except`；禁止一路失败拖垮全局。
- **记录**：失败 **必须** 追加写入 **`sync_errors.log`**（含时间、`adapter_id`、异常摘要）。
- **意义**：例如某一源 API 断连时，其他源仍可入库。

## 3. 严格格式校验（Strict Validation）

- **规则**：唯一合法结构为二级标题（**仅** `## User` 或 `## Assistant`），**换行后紧跟一行**：

  `<!-- captured_at: <ISO8601>Z -->`

  再换行为正文，直至下一角色标题。

- **意义**：机读注释不破坏 Markdown 人读；为 Dify / Python chunking 与块解析提供时间锚点。

## manifest 必填字段

每行 JSON 须含：`session_id`、`source`、`file_path`（相对 `RUNTIME_ROOT` 的 POSIX 路径）、`last_captured_at`。

**`last_captured_at`**：须为 **UTC、以 `Z` 结尾的 ISO8601**（工具写入为秒精度 `YYYY-MM-DDTHH:MM:SSZ`），便于 Qdrant 等引擎做时序范围过滤，并避免跨时区炼化偏差。落盘时由 `dialog_inbox.normalize_manifest_last_captured_at_for_write()` 规范化。

## 低信任区入站

`import_file` 从中转目录等读取源文件，**校验并规范化后写入** `ai_dialogue_inbox/`，不原地信任源路径。

## 与 enseal 衔接（双径）

- **路径 A · 对话归档**：`enseal_skill --materialize-from-inbox <合规 inbox .md> --seal-target-dir <相对 SSOT 根>` → `200_Operations/drafts/<batch>/` Triple-A；**L1 `seal_target_dir` 必填**，否则 `seal-batch` 拒绝。审阅后 `.seal_ready` 或 `approved` → `seal-batch`。
- **路径 B · 认知 Patch**：在 `200_Operations/Cognitive_Patch_Draft.md` 写 `##` 块且正文含 **`Target_SSOT_Path: <相对 SSOT 根>`** → `enseal_skill --materialize-from-cognitive-patch`（`--patch-slug` 或 `--patch-heading-contains`）→ 同上草稿与封印闸门。
- **Dify 炼化**（可选）：工作流产出 Patch 草案或摘要后仍须人工确认；`Target_SSOT_Path` 建议经 `cabinet.path_guardian.check_ssot` 预检。详见 TOOLS [`Manuals/inbox_enseal_seal_runbook.md`](file:///Volumes/Cabinet/cabinet/Cursor_Workspace/Internal_Cabinet_Tools/Manuals/inbox_enseal_seal_runbook.md)、SSOT `Dify/01_Ingest_&_Memory/WF_InboxRefine_Patch.md`。

封印与变更 SSOT **始终**须 Kenny 审批流程。

## 运维策略（PSR / 锁 / 日志巡检）

与 TOOLS [`Manuals/多源对话与PSR运维备忘.md`](file:///Volumes/Cabinet/cabinet/Cursor_Workspace/Internal_Cabinet_Tools/Manuals/%E5%A4%9A%E6%BA%90%E5%AF%B9%E8%AF%9D%E4%B8%8EPSR%E8%BF%90%E7%BB%B4%E5%A4%87%E5%BF%98.md) 同步：**多源共用 PSR**、`inbox_manifest.jsonl` **fcntl 锁**、`health_check.py dialog-sync-errors` **每日巡检**。
